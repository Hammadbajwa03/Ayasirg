import { getServicePageMetadata } from "@/app/lib/serviceChooseContent";

export const metadata = getServicePageMetadata("sofa-carpet-cleaner");

export default function SofaCarpetCleanerLayout({ children }) {
  return children;
}
