import { getServicePageMetadata } from "@/app/lib/serviceChooseContent";

export const metadata = getServicePageMetadata("tyre-specialist");

export default function TyreSpecialistLayout({ children }) {
  return children;
}
