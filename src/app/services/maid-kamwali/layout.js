import { getServicePageMetadata } from "@/app/lib/serviceChooseContent";

export const metadata = getServicePageMetadata("maid-kamwali");

export default function MaidKamwaliLayout({ children }) {
  return children;
}
