import { getServicePageMetadata } from "@/app/lib/serviceChooseContent";

export const metadata = getServicePageMetadata("office-boy-factory-workers");

export default function OfficeBoyFactoryWorkersLayout({ children }) {
  return children;
}
