import { getServicePageMetadata } from "@/app/lib/serviceChooseContent";

export const metadata = getServicePageMetadata("ups-generator-technician");

export default function UPSGeneratorTechnicianLayout({ children }) {
  return children;
}
