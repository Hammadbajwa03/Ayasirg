import { getServicePageMetadata } from "@/app/lib/serviceChooseContent";

export const metadata = getServicePageMetadata("cctv-fence-installer");

export default function CctvFenceInstallerLayout({ children }) {
  return children;
}
