import { getServicePageMetadata } from "@/app/lib/serviceChooseContent";

export const metadata = getServicePageMetadata("blinds-curtains-wallpapers");

export default function BlindsCurtainsWallpapersLayout({ children }) {
  return children;
}
