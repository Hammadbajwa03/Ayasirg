import { getServicePageMetadata } from "@/app/lib/serviceChooseContent";

export const metadata = getServicePageMetadata("ceiling-work");

export default function CeilingWorkLayout({ children }) {
  return children;
}
