import { getServicePageMetadata } from "@/app/lib/serviceChooseContent";

export const metadata = getServicePageMetadata("carpet-cleaning-laundry");

export default function CarpetCleaningLaundryLayout({ children }) {
  return children;
}
